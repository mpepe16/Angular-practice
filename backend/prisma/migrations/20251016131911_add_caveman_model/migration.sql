-- CreateTable
CREATE TABLE "Caveman" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "location" TEXT NOT NULL,
    "age" INTEGER NOT NULL,

    CONSTRAINT "Caveman_pkey" PRIMARY KEY ("id")
);
